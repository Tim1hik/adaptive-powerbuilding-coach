# Deployment

The target deployment platform is Vercel. GitHub Actions run linting, type checks, tests, and builds before merge.

Vercel MCP may be used for deployment checks later. MCP packages must not be runtime app dependencies.
